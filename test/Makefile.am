EXTRA_DIST       = start.sh stop.sh
EXTRA_DIST      += local.sh
CLEANFILES       = *~ *.trs *.log
TEST_EXTENSIONS  = .sh

TESTS            = start.sh
TESTS           += local.sh
TESTS           += stop.sh
